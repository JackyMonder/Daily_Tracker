{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        ".validate": "newData.hasChildren(['name', 'email', 'createdAt', 'updatedAt']) && newData.child('id').val() === $uid && newData.child('email').val().matches(/^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Z|a-z]{2,}$/) && !newData.hasChild('password')"
      }
    },
    "notes": {
      "$noteId": {
        ".read": "auth != null && (!data.exists() || data.child('userId').val() === auth.uid || data.child('userId').val() == null)",
        ".write": "auth != null && (data.child('userId').val() === auth.uid || !data.exists()) && newData.child('userId').val() === auth.uid",
        ".validate": "newData.hasChildren(['title', 'content', 'createdAt', 'updatedAt']) && newData.child('userId').val() === auth.uid && newData.child('title').val().length > 0 && newData.child('title').val().length <= 500 && (newData.child('isDeleted').val() === true || newData.child('isDeleted').val() === false)"
      },
      ".read": "auth != null",
      ".write": false
    },
    
    "test": {
      ".read": true,
      ".write": true
    },
    ".read": false,
    ".write": false
  }
}
